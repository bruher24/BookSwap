type: object
      properties:
        name:
          type: string
          minLength: 3
          maxLength: 30
        by_fields:
          type: string
        is_active:
          type: boolean
            null
      required:
        - name
        - by_fields
      title: StoreFilterRequest
