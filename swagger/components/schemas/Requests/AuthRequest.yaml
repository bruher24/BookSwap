type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
        remember:
          type: boolean
            null
      required:
        - email
        - password
      title: AuthRequest
