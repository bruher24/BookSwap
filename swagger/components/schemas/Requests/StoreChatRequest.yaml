type: object
      properties:
        first_user_id:
          type: integer
        second_user_id:
          type: integer
        blocked_by:
          type: string
            null
          enum: first
            second
      required:
        - first_user_id
        - second_user_id
      title: StoreChatRequest
