type: object
      properties:
        first_user_id:
          type: integer
        second_user_id:
          type: integer
        blocked_by:
          type: string
            null
          enum: first
            second
      required:
        - chat_id
        - first_user_id
        - second_user_id
      title: UpdateChatRequest
