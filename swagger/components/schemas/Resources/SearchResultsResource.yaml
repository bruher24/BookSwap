type: object
      properties:
        books:
          type: array
          items:
            $ref: "#/components/schemas/BookResource"
        authors:
          type: array
          items:
            $ref: "#/components/schemas/AuthorResource"
        total:
          type: integer
      required:
        - books
        - authors
        - total
      title: SearchResultsResource
