type: object
      properties:
        id:
          type: integer
        first_user_id:
          type: integer
        second_user_id:
          type: integer
        is_blocked:
          type: string
        created_at:
          type: string
            null
          format: date-time
        updated_at:
          type: string
            null
          format: date-time
        deleted_at:
          type: string
            null
          format: date-time
      required:
        - id
        - first_user_id
        - second_user_id
        - is_blocked
        - created_at
        - updated_at
        - deleted_at
      title: ChatResource
