description: "Bad Request"
content:
  application/vnd.api+json:
    schema:
      type: object
      properties:
        errors:
          type: array
          items:
            $ref: ../../schemas/api/ErrorObject.yaml
      required:
        - errors
    examples:
      default:
        $ref: examples/BadRequest_Example.yaml
