description: "Not found"
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: string
          description: "Errors overview"
      required:
        - message
    examples:
      default:
        $ref: "examples/ModelNotFoundException_Example.yaml"
