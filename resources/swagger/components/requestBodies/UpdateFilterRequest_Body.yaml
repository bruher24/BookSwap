description: "Filter update payload"
required: true
content:
  application/json:
    schema:
      $ref: "../schemas/requests/UpdateFilterRequest.yaml"
    examples:
      default:
        $ref: "examples/UpdateFilterRequest_Example.yaml"

