description: Refresh request payload
required: true
content:
  application/json:
    schema:
      $ref: "../schemas/requests/RefreshRequest.yaml"
    examples:
      default:
        $ref: "examples/RefreshRequest_Example.yaml"

